apiVersion: v1
kind: Service
metadata:
  name: {{ include "counting.fullname" . }}
  labels:
    {{- include "counting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "counting.selectorLabels" . | nindent 4 }}
