# Dev environment values
replicaCount: 1
image:
  repository: amokscience/hello
  pullPolicy: Always
  tag: "dev-3b5f8c5"

env:
  HTTP_PORT: 8050

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 100Mi

ingress:
  enabled: true
  host: dev.hello.local

serviceAccount:
  create: true
  name: hello

autoscaling:
  enabled: false
